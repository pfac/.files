#!/bin/bash

export SYSTEM=`uname`;
export PATH=".:${HOME}/local/bin:${UNIXCONFDIR}/bin:/usr/local/bin:${PATH}";
export LSCOLORS=Dxfxdxcxbxxxxxxxxxxxxx

# LIBRARY DIRECTORIES
# 	~/lib:	so I can install stuff in my home
# 	/usr/local/cuda/lib:	for CUDA stuff
LIBS="${HOME}/local/lib:/usr/local/cuda/lib";

if [ "$LD_LIBRARY_PATH" ];
then
	export LD_LIBRARY_PATH="${LIBS}:${LD_LIBRARY_PATH}";
else
	export LD_LIBRARY_PATH="${LIBS}";
fi;

PROMPT_COMMAND=setup_ps1;
